<?php

declare(strict_types=1);

/*
| Конфиг Laravel-интеграции (используется src/Laravel/TwigComponentServiceProvider).
| Standalone-подключение в конфиге не нуждается: эти же значения передаются
| аргументами конструктора ComponentRegistry напрямую.
*/

return [

    /*
    |--------------------------------------------------------------------------
    | Базовый неймспейс компонентов
    |--------------------------------------------------------------------------
    | FQCN относительно этого неймспейса даёт имя компонента в реестре
    | (App\View\TwigComponents\Corporate\AgencyParamMenu -> corporate:agency-param-menu).
    | Специфика приложения — пакет сам про App\ не знает.
    */
    'namespace' => 'App\\View\\TwigComponents\\',

    /*
    |--------------------------------------------------------------------------
    | Каталог классов компонентов (для auto-discovery на локали)
    |--------------------------------------------------------------------------
    */
    'path' => app_path('View/TwigComponents'),

    /*
    |--------------------------------------------------------------------------
    | Путь манифеста реестра (прод читает его вместо живого скана)
    |--------------------------------------------------------------------------
    | Пересобирается в php artisan optimize (twig-component:cache).
    */
    'manifest' => base_path('bootstrap/cache/twig-component.php'),

];
